'use strict';

const MODULE_REQUIRE = 1
	/* built-in */
	
	/* NPM */
	
	/* in-package */
	;

module.exports = {
	'options': [
		'--base NOT NULL',
		'--system NOT NULL',
		'--number -n [*] NOT NULL MULTIPLE',
	],
};